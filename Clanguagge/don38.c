#include<stdio.h>
#include<math.h>
int main(int argc, char const *argv[])
{
    int x = 10, y = 15;
    if (x%2== y%3)          /* == is used in if for equality and = for assigning*/
    {
        printf("chalo cahle jabalpur\n");
    }
    return 0;
}

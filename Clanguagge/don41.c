#include<stdio.h>
#include<math.h>
int main(int argc, char const *argv[])
{
int a, b;
printf("enter a\n");
scanf("%d", &a);
b  = a;
if (a == b)
{
    printf("both are equal\n");

    }
else
    {
     printf("both are unequal\n");
    }
    
return 0;
}

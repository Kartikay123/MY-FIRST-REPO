#include<stdio.h>
#include<math.h>
int main(int argc, char const *argv[])
{
    int x = 3, y,z;
    y = x = 10;                 /* for < and > symbols true belongs 1 and false belongs  0*/ 
    z = x <10;
    printf("x = %d\n y= %d\n z = %d", x,y,z);

    return 0;
}
